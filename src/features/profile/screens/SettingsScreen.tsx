import React from 'react'; import {ScreenContainer,AppHeader} from '@components/organisms'; import {Button,Text} from '@components/atoms'; import {Card} from '@components/molecules'; import {useUIStore} from '@app/store/uiStore'; import type {ThemeMode} from '@theme/tokens';
export function SettingsScreen(){const mode=useUIStore(s=>s.themeMode); const set=useUIStore(s=>s.setThemeMode); const options:ThemeMode[]=['system','light','dark']; return <ScreenContainer><AppHeader title="Settings"/><Text variant="title">Appearance</Text><Card>{options.map(item=><Button key={item} title={`${item[0]?.toUpperCase()}${item.slice(1)}${mode===item?' ✓':''}`} variant={mode===item?'primary':'secondary'} onPress={()=>set(item)}/>)}</Card><Text variant="title">Language</Text><Card><Text>English</Text><Text variant="caption">Hindi sample locale included in localization resources.</Text></Card></ScreenContainer>}
