import React from 'react'; import {ScreenContainer,retryConnectivity} from '@components/organisms'; import {EmptyState} from '@components/molecules'; export function OfflineScreen(){return <ScreenContainer><EmptyState title="You’re offline" message="Reconnect to load fresh data. Previously loaded tasks remain available." action={{label:'Try again',onPress:()=>void retryConnectivity()}}/></ScreenContainer>}
