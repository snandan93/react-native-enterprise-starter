import React from 'react'; import {NavigationContainer,DarkTheme,DefaultTheme} from '@react-navigation/native'; import {createNativeStackNavigator} from '@react-navigation/native-stack'; import {createBottomTabNavigator} from '@react-navigation/bottom-tabs'; import {useTheme} from '@app/providers/AppProviders'; import {useAppSelector} from '@hooks/redux'; import {SplashScreen} from '@features/auth/screens/SplashScreen'; import {LoginScreen} from '@features/auth/screens/LoginScreen'; import {ForgotPasswordScreen} from '@features/auth/screens/ForgotPasswordScreen'; import {DashboardScreen} from '@features/dashboard/screens/DashboardScreen'; import {TaskListScreen} from '@features/tasks/screens/TaskListScreen'; import {TaskDetailsScreen} from '@features/tasks/screens/TaskDetailsScreen'; import {TaskFormScreen} from '@features/tasks/screens/TaskFormScreen'; import {ProfileScreen} from '@features/profile/screens/ProfileScreen'; import {SettingsScreen} from '@features/profile/screens/SettingsScreen'; import type {AuthStackParams,MainTabsParams,TaskStackParams} from './types';
const Auth=createNativeStackNavigator<AuthStackParams>(); const Tasks=createNativeStackNavigator<TaskStackParams>(); const Tabs=createBottomTabNavigator<MainTabsParams>();
function AuthNav(){return <Auth.Navigator><Auth.Screen name="Login" component={LoginScreen} options={{headerShown:false}}/><Auth.Screen name="ForgotPassword" component={ForgotPasswordScreen} options={{title:'Forgot password'}}/></Auth.Navigator>}
function TaskNav(){return <Tasks.Navigator><Tasks.Screen name="TaskList" component={TaskListScreen} options={{headerShown:false}}/><Tasks.Screen name="TaskDetails" component={TaskDetailsScreen} options={{title:'Details'}}/><Tasks.Screen name="TaskForm" component={TaskFormScreen} options={{title:'Task'}}/></Tasks.Navigator>}
function MainNav(){return <Tabs.Navigator screenOptions={{headerShown:false}}><Tabs.Screen name="Dashboard" component={DashboardScreen}/><Tabs.Screen name="Tasks" component={TaskNav}/><Tabs.Screen name="Profile" component={ProfileScreen}/><Tabs.Screen name="Settings" component={SettingsScreen}/></Tabs.Navigator>}
export function AppNavigator(){const status=useAppSelector(s=>s.auth.status); const theme=useTheme(); const navTheme={...(theme.dark?DarkTheme:DefaultTheme),colors:{...(theme.dark?DarkTheme:DefaultTheme).colors,primary:theme.colors.primary,background:theme.colors.background,card:theme.colors.surface,text:theme.colors.text,border:theme.colors.border}}; if(status==='restoring')return <SplashScreen/>; return <NavigationContainer theme={navTheme}>{status==='authenticated'?<MainNav/>:<AuthNav/>}</NavigationContainer>}
